#pragma once //preprocessor directive ensures only defined once.
            // similar to #ifdef and #ifndef.

//functions declaration or prototypes
double power_of(double num, int exponent);

bool is_prime(int num);